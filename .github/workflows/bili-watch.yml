
Skip to content

    wlb20144148
    bili-up-watch

Repository navigation

Code
Issues
Pull requests
Agents
Actions
Projects
Wiki
Security and quality
Insights

    Settings

.github/workflows/bili-watch.yml
.github/workflows/bili-watch.yml #2

All jobs

Run details

Workflow file for this run
.github/workflows/bili-watch.yml at b561873
on:

Check failure on line 1 in .github/workflows/bili-watch.yml
GitHub Actions / .github/workflows/bili-watch.yml

Invalid workflow file

(Line: 5, Col: 3): Unexpected value 'contents', (Line: 5, Col: 13): Unexpected value 'write', (Line: 9, Col: 5): Required property is missing: runs-on

  schedule:
    - cron: "*/30 * * * *"
  workflow_dispatch:
  contents: write

jobs:
  watch:
    steps:
      - uses: actions/checkout@v4
        with:
          python-version: "3.11"

      - run: pip install -r requirements.txt

      - run: python watch.py
        env:
          WXPUSHER_SPT: ${{ secrets.WXPUSHER_SPT }}

      - name: Commit state
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add seen.json
          git diff --cached --quiet || git commit -m "Update seen videos"
          git push
